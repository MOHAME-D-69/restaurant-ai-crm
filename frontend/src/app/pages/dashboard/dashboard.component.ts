import { CommonModule } from '@angular/common'; import { Component, OnInit, inject } from '@angular/core'; import { ApiService } from '../../core/api.service';
@Component({standalone:true,imports:[CommonModule],template:`<div class="page-head"><div><h2>Dashboard</h2><p>Overview of your restaurant performance.</p></div><span class="live">● Live data</span></div><div class="cards"><div class="stat"><span>Orders Today</span><strong>{{dashboard.ordersToday}}</strong><small>All orders created today</small></div><div class="stat"><span>Revenue Today</span><strong>{{dashboard.revenueToday|number}} EGP</strong><small>Excluding cancelled orders</small></div><div class="stat"><span>Customers</span><strong>{{dashboard.customers}}</strong><small>Total customers</small></div><div class="stat"><span>Avg Order</span><strong>{{dashboard.averageOrderValue|number}} EGP</strong><small>Delivered orders</small></div></div><div class="grid-2"><div class="panel"><div class="panel-title"><div><h3>Revenue</h3><small class="panel-subtitle">Last {{selectedDays}} days</small></div><select [value]="selectedDays" (change)="changePeriod($any($event.target).value)"><option value="7">7 days</option><option value="30">30 days</option><option value="90">90 days</option></select></div><div class="bars"><div class="bar-wrap" *ngFor="let item of visibleRevenue"><div class="bar-value">{{item.revenue|number}}</div><div class="bar" [style.height.px]="barHeight(item.revenue)"></div><small>{{item.date|date:'EEE'}}</small></div></div></div><div class="panel"><div class="panel-title"><div><h3>Top products</h3><small class="panel-subtitle">Best sellers</small></div></div><div class="rank" *ngFor="let item of dashboard.topProducts;let i=index"><span class="rank-no">{{i+1}}</span><div><strong>{{item.name}}</strong><small>{{item.quantity}} sold</small></div><b>{{item.revenue|number}} EGP</b></div><div class="empty-table" *ngIf="!dashboard.topProducts.length">No delivered orders yet.</div></div></div><div class="grid-2 dashboard-lower-grid"><div class="panel"><div class="panel-title"><div><h3>Order Status</h3><small class="panel-subtitle">Current order distribution</small></div></div><div class="status-list"><div class="status-row" *ngFor="let item of dashboard.orderStatus"><div class="status-name"><span class="status-dot" [ngClass]="item.className"></span><span>{{item.label}}</span></div><strong>{{item.value}}</strong></div></div></div><div class="panel"><div class="panel-title"><div><h3>Recent Orders</h3><small class="panel-subtitle">Latest activity</small></div></div><div class="recent-order" *ngFor="let order of dashboard.recentOrders"><div><strong>{{order.orderId}}</strong><small>{{order.customer}}</small></div><div class="recent-order-right"><strong>{{order.total|number}} EGP</strong><span [ngClass]="statusClass(order.status)">{{order.status}}</span></div></div><div class="empty-table" *ngIf="!dashboard.recentOrders.length">No orders yet.</div></div></div>`})
export class DashboardComponent implements OnInit {private api=inject(ApiService);dashboard:any={ordersToday:0,revenueToday:0,customers:0,averageOrderValue:0,revenue:[],topProducts:[],orderStatus:[],recentOrders:[]};selectedDays=7;ngOnInit(){this.load();}load(){this.api.get<any>('/dashboard/overview').subscribe({next:r=>this.dashboard={...this.dashboard,...r}});this.api.get<any[]>(`/dashboard/revenue?days=${this.selectedDays}`).subscribe({next:r=>this.dashboard.revenue=r});this.api.get<any[]>('/dashboard/top-products').subscribe({next:r=>this.dashboard.topProducts=r});this.api.get<any[]>('/orders').subscribe({next:orders=>{const statuses=['Pending','Confirmed','In Kitchen','Out for Delivery','Delivered','Cancelled'];this.dashboard.orderStatus=statuses.map(label=>({label,value:orders.filter(o=>o.status===label).length,className:this.statusDotClass(label)}));this.dashboard.recentOrders=orders.slice(0,5).map(o=>({orderId:'#'+o.order_id,customer:o.customer_name,total:Number(o.total||0),status:o.status}));}});}changePeriod(v:string){this.selectedDays=Number(v);this.api.get<any[]>(`/dashboard/revenue?days=${this.selectedDays}`).subscribe({next:r=>this.dashboard.revenue=r});}get visibleRevenue(){return this.dashboard.revenue||[];}barHeight(v:number){const max=Math.max(...this.visibleRevenue.map((x:any)=>Number(x.revenue)),1);return Math.max(18,Math.round((Number(v)/max)*150));}statusDotClass(s:string){const m:any={Pending:'status-pending',Confirmed:'status-confirmed','In Kitchen':'status-kitchen','Out for Delivery':'status-delivery',Delivered:'status-delivered',Cancelled:'status-cancelled'};return m[s]||'';}statusClass(s:string){const m:any={Delivered:'order-status delivered','In Kitchen':'order-status kitchen',Pending:'order-status pending','Out for Delivery':'order-status delivery',Confirmed:'order-status confirmed',Cancelled:'order-status cancelled'};return m[s]||'order-status';}}
